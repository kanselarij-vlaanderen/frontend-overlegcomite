{{page-title "Zoeken"}}

<div class="au-o-region au-u-background-white">
  <div class="au-o-flow au-o-flow--small au-o-layout">
    <AuDataTable
      @content={{@model.data}}
      @fields="meetingDate subject"
      @noDataMessage="Geen agendapunten gevonden"
      @sort={{this.sort}}
      @page={{this.page}}
      @size={{this.size}}
      as |t|>
      <t.menu as |menu|>
        <menu.general>
          <div class="au-o-flow au-o-flow--small au-u-margin-bottom-small">
            <AuHeading @level=1 @skin=2>
              Agendapunten zoeken
            </AuHeading>
            <AuHr />
            <div class="au-o-grid au-o-grid--small au-o-grid--middle">
              <div class="au-o-grid__item au-u-1-2@medium au-u-2-3@small">
                <AuDataTable::TextSearch
                  @filter={{this.searchText}} />
              </div>
              <div class="au-o-grid__item au-u-1-2@mediumi au-u-1-3@small">
                <AuToggleSwitch
                  @checked={{this.notificationsOnly}}
                  @onChange={{fn (mut this.notificationsOnly)}}
                  >
                  Uitsluitend in notificaties zoeken
                </AuToggleSwitch>
              </div>
            </div>
          </div>
        </menu.general>
      </t.menu>
      <t.content as |c|>
        <c.header>
          <AuDataTable::ThSortable
            @field="meetingDate"
            @currentSorting={{this.sort}}
            @label="Vergadering"
            />
          <th>
            Onderwerp
          </th>
          <th />
        </c.header>
        <c.body as |agendaitem|>
          <td>
            {{format-datetime agendaitem.attributes.meetingDate}}
          </td>
          <td>
            {{agendaitem.attributes.subject}}
          </td>
          <td class="au-u-text-center">
            <AuLink
              @route="meeting.agendaitems.agendaitem"
              @models={{array (or agendaitem.attributes.meetingId 'NO_MEETING_ID') agendaitem.id}}
              @icon="chevron-right"
              @hideText={{true}}>
              Ga naar agendapunt
            </AuLink>
          </td>
        </c.body>
      </t.content>
    </AuDataTable>
  </div>
</div>
