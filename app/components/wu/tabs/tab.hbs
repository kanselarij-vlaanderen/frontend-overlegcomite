<LinkTo @route={{@route}}
        @current-when={{@current-when}}
        @tagName="li" @activeClass="vl-tab--active" class="vl-tab"
>
  <LinkTo @route={{@route}}
          class="vl-tab__link" role="tab" aria-selected="true"
  >
    {{yield}}
  </LinkTo>
</LinkTo>